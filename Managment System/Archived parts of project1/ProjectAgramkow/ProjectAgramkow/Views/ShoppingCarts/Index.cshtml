@model IEnumerable<ProjectAgramkow.Models.ShoppingCart>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Amount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FoodItem.ItemName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Receipt.ReceiptNumber)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Amount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FoodItem.ItemName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Receipt.ReceiptNumber)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ShoppingCartId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ShoppingCartId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ShoppingCartId })
        </td>
    </tr>
}

</table>
