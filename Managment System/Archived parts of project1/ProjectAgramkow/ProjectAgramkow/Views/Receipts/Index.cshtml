@model IEnumerable<ProjectAgramkow.Models.Receipt>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ReceiptCreated)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Employee.EmpName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ReceiptCreated)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Employee.EmpName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ReceiptNumber }) |
            @Html.ActionLink("Details", "Details", new { id=item.ReceiptNumber }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ReceiptNumber })
        </td>
    </tr>
}

</table>
