@model IEnumerable<ProjectAgramkow.Models.FoodCategory>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CategoryName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CategoryName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CategoryNumber }) |
            @Html.ActionLink("Details", "Details", new { id=item.CategoryNumber }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CategoryNumber })
        </td>
    </tr>
}

</table>
